import { components } from '@/themes/config/components';
import { typography } from '@/themes/config/typography';
import palette from './palette';

export const themeConfig = {
  palette,
  typography,
  components,
};

export { palette, typography, components };
